server {
    listen 8080;
    listen [::]:8080;
    root /usr/share/nginx/html;
    index index.html;
    absolute_redirect off;

    # each version is one ~135 KB HTML file; gzip brings it to about 42 KB.
    # Fly's proxy adds a Via header, and nginx skips proxied requests unless told otherwise.
    gzip on;
    gzip_proxied any;
    gzip_comp_level 6;
    gzip_min_length 1024;
    gzip_types text/css application/javascript text/plain;

    location / {
        try_files $uri $uri/ =404;
        add_header Cache-Control "public, max-age=600";
        add_header X-Content-Type-Options "nosniff";
    }
}
